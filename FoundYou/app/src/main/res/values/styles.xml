<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/verde</item>
        <item name="colorPrimaryDark">@color/verdeEscuro</item>
        <item name="colorAccent">@color/verde</item>
        <item name="android:textColorPrimary">@color/verdeEscuro</item>
    </style>


    <!-- Thema Activity Publicação -->
    <style name="AppThemePublicacao" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorPrimary">@color/verde</item>
        <item name="colorPrimaryDark">@color/verdeEscuro</item>
        <item name="colorAccent">@color/verde</item>
    </style>

    <!-- Thema Edit Text -->
    <style name="EditTextPersonalizado">
        <item name="colorControlActivated">@color/verde</item>
        <item name="colorControlHighlight">@color/verdeEscuro</item>
        <item name="android:textColorHint">@color/verde</item>
        <item name="android:singleLine">true</item>
    </style>

    <!-- Thema Radio Button -->
    <style name="RadioButtonPersonalizado">
        <item name="colorControlNormal">@color/cinzaClaro</item>
        <item name="colorControlActivated">@color/verde</item>
        <item name="colorControlHighlight">@color/verdeEscuro</item>
    </style>

    <!-- Thema Spinner -->
    <style name="SpinnerPersonalizado">
        <item name="colorControlNormal">@color/verde</item>
        <item name="colorControlActivated">@color/verde</item>
        <item name="colorControlHighlight">@color/verde</item>
        <item name="android:textColor">@color/verde</item>
    </style>

    <!-- Thema ProgressBar -->
    <style name="ProgressBarPersonalizado">
        <item name="colorControlNormal">@color/verde</item>
        <item name="colorControlActivated">@color/verde</item>
        <item name="colorControlHighlight">@color/verde</item>
        <item name="android:textColor">@color/verde</item>
    </style>


    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light">
    <item name="windowNoTitle">true</item>
    <item name="windowActionBar">false</item>
    <item name="android:windowFullscreen">true</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="colorAccent">@color/verde</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
